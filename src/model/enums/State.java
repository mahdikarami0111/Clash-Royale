package model.enums;

/**
 * state of units can be one of five:
 * dead, attacking, moving_left, moving_right, idle(none of the others)
 */
public enum State {
    DEAD,
    ATTACKING,
    MOVING_LEFT,
    MOVING_RIGHT,
    IDLE,
}
